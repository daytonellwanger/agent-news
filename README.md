# Structure

The `wiki-maintainer` directory contains everything the agent needs to maintain the wiki. Everything else in this repo supports building out that directory. E.g., instructions on how to update the wiki belong in `wiki-maintainer`; but instructions on how to update the instructions belong outside of it.
